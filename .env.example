# No environment variables are required for this site.
# It is a fully static single-page app deployed on Cloudflare Pages.
# Build runs with: npm install && npm run build (or bun install && bun run build)